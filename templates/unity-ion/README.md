# Walking skeleton — Cesium for Unity + ion

After the spec is approved:

- Unity project the spec named (version pin)
- Official Cesium for Unity package
- CesiumGeoreference + one ion tileset
- Domain assembly without Cesium types
- Token in env, not in git

Do not copy this folder until TRACK is `unity-ion`.
