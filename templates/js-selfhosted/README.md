# Walking skeleton — CesiumJS + self-hosted 3D Tiles

After the spec is approved:

- Vite + TypeScript
- Viewer loads a tileset URL from env
- No ion token required for the skeleton
- Domain types independent of Viewer
- Vitest for domain without WebGL

Do not copy this folder until TRACK is `js-selfhosted`.
