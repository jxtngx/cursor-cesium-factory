# Walking skeleton — CesiumJS + local tiles / GeoJSON

After the spec is approved:

- Vite + TypeScript
- Viewer with ellipsoid or bundled terrain fixture
- Local GeoJSON / CZML / small tileset in `fixtures/` (public-domain only)
- Vitest for domain without WebGL

Do not copy this folder until TRACK is `js-local`.
