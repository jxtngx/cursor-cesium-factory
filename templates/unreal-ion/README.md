# Walking skeleton — Cesium for Unreal + ion

After the spec is approved:

- Unreal project the spec named (version pin)
- Official Cesium for Unreal plugin
- CesiumGeoreference + one ion tileset
- Domain in a module that does not include Viewer types
- Token in env / editor settings, not in git

Do not copy this folder until TRACK is `unreal-ion`.
