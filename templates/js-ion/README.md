# Walking skeleton — CesiumJS + ion

After the spec is approved:

- Vite + TypeScript
- Viewer with ion terrain/imagery the spec named
- One 3D Tileset from an ion asset id in env
- Domain types for the first entity list
- Vitest for domain without WebGL

Do not copy this folder until TRACK is `js-ion`.
