# Platform spec template

Write to `.cursor/plans/project-init/[slug]-platform.plan.md`.

```markdown
# [name] — Platform

- TRACK:
- Runtime / version pin:
- Data host (ion asset ids or tileset URL; never the token):
- Default cartographic view:
- Terrain / imagery:
- Vector tiles: [mvp | later | no]
- Host: [Vite | Unreal version | Unity version]
- Performance budget (frame time / tile budget) if the spec named one:
```
